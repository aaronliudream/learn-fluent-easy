# 【CC执行】成人英语板块替换为美语课程 v1.0
> 本指令是《CC执行_美语课程落地指令_v1.md》的前置/并行任务。
> 决策：首页"成人英语"卡片整体替换为美语课程入口，原成人英语内容全部下线。

## 0. 硬约束

- 禁触四文件铁律照旧（PrimaryHub 系列）。
- 遵循站内既定流程：**先盘点 → 匿名只读备份 → 再清理**，禁止直接删。
- 清理 SQL 全部进 `SQLAA/`（命名 `adult_phase0_backup.sql` / `adult_phase1_clear.sql`），由 Aaron service-role 执行，CC 不直接删线上数据。
- 分支：并入 `feat-american-course`。
- 卡片标题占位为「美语新起点」，最终名称等 Aaron 一句话确认后全局替换（标题只允许出现在一个常量里，方便改名）。
- 页面与代码中不得出现"新概念 / NCE"字样。

## Phase 0：盘点 + 备份（先做，输出报告后再动）

1. 盘点成人英语板块现状并输出清单：
   - 路由（/adult? /cefr? 实际路径以代码为准）及全部页面组件
   - 涉及数据表（adult_* / cefr_* 或其他命名，以实际为准）
   - 共享依赖检查：确认成人板块组件是否被其他专区引用（alexVoice.ts / TappableLine.tsx 教训——若有共享，只解除引用，不删共享件）
   - 是否有 edge function 专属于成人板块（有则列出，先不删）
2. 数据表匿名只读全量备份（同 gaokao Phase 0 做法），备份表加 RLS 锁死（参照此前 13 张备份表锁定方案）。
3. 输出盘点报告给 Aaron 确认后，再进 Phase 1。

## Phase 1：下线与清理

1. 生成 `adult_phase1_clear.sql` 到 SQLAA/（清内容数据；用户历史数据保留在备份中）。
2. 前端：删除/归档成人板块页面组件（归档到 `_archived/adult/`，不直接物理删除，回滚成本低）。
3. 旧成人路由 301/redirect 到 `/american`（防外部收藏链接 404）。

## Phase 2：首页卡片替换

原卡片（成人英语 · 职场口语 CEFR 分级 · A1-C2）替换为：

- **标题**：美语新起点（常量占位，待最终定名）
- **副标题**：地道美语 从零开始
- **左下标签**：72课 · 12单元（替代原 "A1 - C2"）
- **按钮**：去学习 → 指向 `/american`
- 右上角大学徽标位：更换为美国地标风格图（不用真实大学商标/校名——顺带提醒：现有各卡片使用斯坦福/哈佛等校名与照片本身有商标肖像风险，本次先不动其他卡，仅新卡避开）
- 卡片视觉风格与其余三张一致（暗色底图 + 渐变遮罩），断点表现在手机端自测

## Phase 3：回归点检

1. 首页四卡布局手机/桌面不破版
2. 旧成人路由跳转 /american 正常
3. 小学/初中/高中三区导航、路由零影响
4. 全站 grep "新概念|NCE" 无结果（产品面）

## 交付

盘点报告（Phase 0 后即交）→ Aaron 确认 → 执行 Phase 1–3 → 变更清单 + SQLAA 待执行清单 → Aaron 真机验收（合并进美语课程验收清单一起验）。
